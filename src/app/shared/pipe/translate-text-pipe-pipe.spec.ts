import { TranslateTextPipePipe } from './translate-text-pipe-pipe';

describe('TranslateTextPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateTextPipePipe();
    expect(pipe).toBeTruthy();
  });
});
