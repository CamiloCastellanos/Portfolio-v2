import { TranslocoGlobalConfig } from '@ngneat/transloco-utils/src/lib/transloco-utils.types';

const config: TranslocoGlobalConfig = {
  rootTranslationsPath: 'src/assets/i18n/',
  langs: ['en', 'es'],
  keysManager: {}
};

export default config;
